{
  "private": true,
  "scripts": {
    "start": "web-ext run",
    "build": "web-ext build",
    "test": "prettier --check . && npm run build"
  },
  "devDependencies": {
    "prettier": "2.6.1",
    "web-ext": "6.8.0"
  }
}
